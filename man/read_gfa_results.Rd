% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_gfa_results.R
\name{read_gfa_results}
\alias{read_gfa_results}
\title{Read GFA Analysis Results}
\usage{
read_gfa_results(out_prefix = "gfa_output")
}
\arguments{
\item{out_prefix}{Character string specifying the prefix used when running \code{\link[=run_gfa]{run_gfa()}}.
Default is "gfa_output". The function will look for TSV files matching the pattern
\verb{\{out_prefix\}_\{motif_type\}.tsv}.}
}
\value{
A named list of data frames, where each name corresponds to a motif type
and the data frame contains the results for that motif type. Possible names include:
\itemize{
\item \code{APR} - A-Phased Repeats: repeating pattern of A and T bases
\item \code{DR} - Direct Repeats: identical sequences in direct orientation
\item \code{GQ} - G-Quadruplexes: G4 structures with four guanine planes
\item \code{IR} - Inverted Repeats: sequences followed by reverse complement
\item \code{MR} - Mirror Repeats: palindromic repeat patterns
\item \code{STR} - Short Tandem Repeats: microsatellites
\item \code{Z} - Z-DNA: left-handed DNA conformation
}
Only data frames for motif types with existing output files are included.
Each data frame contains tab-separated values with a header row.
}
\description{
Reads and parses TSV output files produced by the GFA program for a given analysis run.
}
\details{
The function reads TSV files produced by \code{\link[=run_gfa]{run_gfa()}} and returns them as a named list.
If the GFA program did not find any instances of a particular motif type,
the corresponding TSV file may not exist, and it will not be included in the result.

The function requires at least one TSV file to exist for the given \code{out_prefix}.
If no TSV files are found, an error is raised.
}
\examples{
\dontrun{
  # Run GFA analysis
  run_gfa("sequences.fasta", "my_run")

  # Read the results
  results <- read_gfa_results("my_run")

  # Examine the structure
  str(results)
  names(results)

  # Access individual motif types
  apr_data <- results$APR
  gq_data <- results$GQ

  # Check which motifs were found
  if (length(results) > 0) {
    cat("Found", length(results), "motif types\n")
  }
}

}
\seealso{
\code{\link[=run_gfa]{run_gfa()}} for running the GFA analysis program.
}
\keyword{dna-motifs}
\keyword{genomics}
\keyword{io}
